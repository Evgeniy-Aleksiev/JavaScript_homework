function stringLength(arr1, arr2, arr3){
    let len = arr1.length + arr2.length + arr3.length;
    console.log(len)
    console.log(Math.floor(len / 3))
};

stringLength('chocolate', 'ice cream', 'cake');